import { db } from '@/db'
import { logger } from '@/libs/logger'

// All auth utilities for old email/password system have been removed.
// Google OAuth handles everything now.
// See src/libs/google.ts for token verification.

export { }
